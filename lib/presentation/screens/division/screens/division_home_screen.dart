import 'package:flutter/material.dart';

class DivisionHomeScreen extends StatelessWidget {
  const DivisionHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('DIVISION'),);
  }
}